export const caliberOptions = [1.15, 1.18, 1.20, 1.23, 1.25, 1.28, 1.30];

export const racketOptions = [];
export const stringsOptions = [];
export const tensionOptions = Array.from({ length: 60 - 11 + 1 }, (_, i) =>
  (60 - i)
);

